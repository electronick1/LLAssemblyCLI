
Stage 2:

The control-flow plan could not be loaded:
  {error}

This is the worker's file to fix. SPAWN A NEW AGENT — never a continuation of one that
already returned — and copy the error text above into its prompt verbatim, so it fixes
exactly that instead of redrawing the plan from scratch.

Do not edit it yourself. SPAWN A NEW AGENT to run the worker file again, quoting
the error above:
