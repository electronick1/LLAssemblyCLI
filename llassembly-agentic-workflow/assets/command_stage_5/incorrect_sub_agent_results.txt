
# Stage 5. Follow instructions:

The result file for sub-agent `{agent_name}`, defined at `{agent_path}`, is missing, is not
valid JSON, or does not carry the expected keys.

- The result must be one JSON object with exactly the keys under `properties` in this schema:
{output_schema}

Do not write the result file yourself or supply any of its values.
SPAWN A NEW AGENT for `{agent_name}` — never a continuation of the one that already ran.
Tell it to read `{agent_path}`, do the work, and write that JSON to `{result_path}` with a
file-writing tool. The values are the sub-agent's to determine.
If the plan looped back after a rejection, quote the rejecting sub-agent's report verbatim
into the new dispatch; the driver has already recorded it.

Then re-run `python scripts/get_next_instruction.py --llassembly-session {workflow_id}`
with NO arguments. Never write an output value yourself — not on a command line, not in a
script.
