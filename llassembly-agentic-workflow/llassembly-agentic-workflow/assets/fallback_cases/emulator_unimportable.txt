
The workflow cannot start: `scripts/emulator.py` needs the module `{module_name}`,
which this python ({executable}) cannot import.

This may be the wrong python, or a broken install. If the user or the project named a
specific interpreter, re-run this same command once with it. Otherwise it is not yours
to repair: do not install anything, do not set PYTHONPATH, do not edit the skill.
Report this and stop — no stage was reached, so do not produce the deliverable yourself.