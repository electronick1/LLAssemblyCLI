
# Stage 3. Follow instructions:

## Your only job on this stage is to:

1) Generate exactly {count} sub-agent definition(s) - all in parallel: SPAWN ONE NEW AGENT (worker)
per sub-agent listed below, each running `references/workers/llassembly-generate-sub-agents.md`
(under the `llassembly-agentic-workflow` skill), in one batch (emit all the spawn calls in
one turn); each new agent writes one sub-agent definition file. You do not read that worker
file, create any directory, or write a definition file yourself.
2) As an orchestrator after the above workers are finished you must continue, re-run:
`python scripts/get_next_instruction.py --llassembly-session {workflow_id}`
with no further arguments, and follow next instructions.


## Sub-agents definitions:

```
{sub_agents_block}
```

## How to delegate work to a new agent:

1) For each sub-agent define the prompt that will be sent to the new agent.

2) In that prompt you must include the text (intended for the worker, not
for you), stating the following (all statements required — keep their wording as printed;
fill in the placeholders and add the goal details, but do not reword or shorten them):
```
- You are a worker inside an already running `llassembly-agentic-workflow` skill, not
the orchestrator. You do the work specified and exit right after that.
- As a worker you are not allowed to run scripts/get_next_instruction.py, this is not your scope!
- Writing the sub-agent definition file is your only job. As soon as it is written your job is done - exit immediately.
- Your scope of work is to write that definition following
`references/workers/llassembly-generate-sub-agents.md` from the `llassembly-agentic-workflow` skill
for the following sub-agent:
--- Sub-agent definition ---
sub_agent_name: <sub-agent-name>
sub_agent_path: <sub-agent-path>
sub_agent_objective: <sub-agent-objective>
The plan that invokes it: `{plan_path}`
```

3) You must fill in definition (replacing the `<...>` placeholders) for each new agent
separately, based on the "Sub-agents definitions" section above.

## Wait for all spawned agents to finish, when all agents are done:

Immediately re-run:
`python scripts/get_next_instruction.py --llassembly-session {workflow_id}`
seeking for the next instruction and following what it says.


